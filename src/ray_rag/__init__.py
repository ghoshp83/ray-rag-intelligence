"""ray_rag — distributed RAG document-intelligence platform on Ray.

Traditional ML owns retrieval ranking (cross-encoder reranker) and query
routing (intent classifier); the LLM only does citation-grounded generation.
"""

__version__ = "0.1.0"
